package recipes.util;

public class StringPool {
    public static final String RECIPE_NOT_FOUND = "Not found recipe with id = %d";

    private StringPool() {
        throw new IllegalStateException("Utility class");
    }
}